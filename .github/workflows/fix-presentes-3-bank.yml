name: Test workflow temporal
"on":
  push:
    paths:
      - '.github/triggers/fix-presentes-3-bank.txt'
permissions:
  contents: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo OK
