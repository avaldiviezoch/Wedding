import { db, getWeddingContext } from '../../services/firebase.js?v=20260814-1136-collab1';
import { doc as fsDoc, getDoc, serverTimestamp, writeBatch } from 'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';

const VERSION='20260816-1920-music-fast1';
const PUBLIC_RSVP_BASE='https://avaldiviezoch.github.io/Wedding/rsvp.html';
const NATIVE_WIDGET_URL='https://avaldiviezoch.github.io/Wedding/app_integral/js/modules/invitados/rsvp-native-widget.js?v=20260820-5b2';
const done=new WeakSet();
const DEFAULTS={enabled:true,title:'La música también la eligen ustedes',intro:'Ayúdanos a preparar la fiesta. Déjanos las canciones que te gustaría escuchar para tenerlas en cuenta con el DJ o grupo y que ese día solo tengas que disfrutar.',maxSongs:5,askArtist:true,askMessage:true,messageLabel:'Mensaje o dedicatoria (opcional)'};

function clean(value,max){return String(value??'').trim().slice(0,max);}
function config(value={}){return{enabled:value.enabled!==false,title:clean(value.title||DEFAULTS.title,110)||DEFAULTS.title,intro:clean(value.intro||DEFAULTS.intro,500)||DEFAULTS.intro,maxSongs:Math.max(1,Math.min(10,Math.floor(Number(value.maxSongs)||5))),askArtist:value.askArtist!==false,askMessage:value.askMessage!==false,messageLabel:clean(value.messageLabel||DEFAULTS.messageLabel,90)||DEFAULTS.messageLabel};}
function esc(value=''){return String(value).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');}
function musicUrl(token){return token?`${PUBLIC_RSVP_BASE}?token=${encodeURIComponent(token)}&view=music`:'';}
function embed(token){const t=clean(token,180);return t?`<div\n  data-mgd-music-token="${t}"\n  style="--mgd-accent:#6d7559;--mgd-surface:rgba(255,255,255,.12);--mgd-border:rgba(109,117,89,.24);"\n></div>\n<script type="module" src="${NATIVE_WIDGET_URL}"></script>`:'';}

function ensureHelp(doc,area){
  const box=area?.closest('.mgd-music-integrate-box');if(!box)return;
  if(!doc.getElementById('mgdMusicNativeHelpStyles')){const st=doc.createElement('style');st.id='mgdMusicNativeHelpStyles';st.textContent='.mgd-music-native-help{margin-top:12px;padding:14px 15px;border:1px solid #e1dfd7;border-radius:14px;background:#faf9f6;color:#62695d;font-size:11px;line-height:1.55}.mgd-music-native-help strong{display:block;margin-bottom:7px;color:#343b31}.mgd-music-native-help ol{margin:0 0 8px 18px;padding:0}.mgd-music-native-help code{padding:1px 4px;border-radius:5px;background:#eef0e6;color:#59634f}';doc.head.appendChild(st);}
  if(!box.querySelector('[data-mgd-native-help="music"]'))box.insertAdjacentHTML('beforeend','<div class="mgd-music-native-help" data-mgd-native-help="music"><strong>Cómo agregar Música a tu invitación</strong><ol><li>Copia el <b>Código nativo Mi Gran Día</b>.</li><li>Pégalo donde quieras que aparezca la encuesta.</li><li>No cambies el token ni la URL del script.</li></ol><p>Las canciones quedarán vinculadas a esta boda y al RSVP del invitado.</p></div>');
}
function readUi(doc){return config({enabled:!!doc.getElementById('mgdMusicEnabled')?.checked,title:doc.getElementById('mgdMusicTitle')?.value,intro:doc.getElementById('mgdMusicIntro')?.value,maxSongs:doc.getElementById('mgdMusicMaxSongs')?.value,askArtist:!!doc.getElementById('mgdMusicAskArtist')?.checked,askMessage:!!doc.getElementById('mgdMusicAskMessage')?.checked,messageLabel:doc.getElementById('mgdMusicMessageLabel')?.value});}
function preview(doc,c=readUi(doc)){const host=doc.getElementById('mgdMusicPreview');if(!host)return;host.innerHTML=`<small>Vista previa</small><strong>${esc(c.title)}</strong><p>${esc(c.intro)}</p><div class="mgd-music-preview-fields"><div class="mgd-music-preview-field">Canción</div>${c.askArtist?'<div class="mgd-music-preview-field">Artista</div>':''}${c.askMessage?`<div class="mgd-music-preview-field wide">${esc(c.messageLabel)}</div>`:''}</div>`;}
function fill(doc,c){const set=(id,v)=>{const el=doc.getElementById(id);if(el)el.value=String(v??'');};set('mgdMusicTitle',c.title);set('mgdMusicIntro',c.intro);set('mgdMusicMaxSongs',c.maxSongs);set('mgdMusicMessageLabel',c.messageLabel);const enabled=doc.getElementById('mgdMusicEnabled');if(enabled)enabled.checked=c.enabled;const artist=doc.getElementById('mgdMusicAskArtist');if(artist)artist.checked=c.askArtist;const message=doc.getElementById('mgdMusicAskMessage');if(message)message.checked=c.askMessage;preview(doc,c);}
function integration(doc,token){const u=doc.getElementById('mgdMusicPublicUrl');if(u)u.value=musicUrl(token);const e=doc.getElementById('mgdMusicEmbedCode');if(e){e.value=embed(token);e.placeholder='Aquí aparecerá el código nativo para integrar';const label=e.closest('.mgd-music-integrate-box')?.querySelector('label');if(label)label.textContent='Código nativo Mi Gran Día </>';ensureHelp(doc,e);}['mgdCopyMusicUrl','mgdOpenMusicUrl','mgdCopyMusicEmbed'].forEach(id=>{const b=doc.getElementById(id);if(b)b.disabled=!token;});}
async function load(doc){const ctx=getWeddingContext();if(!ctx?.id||ctx.legacyMode)return;const snap=await getDoc(fsDoc(db,'weddings',ctx.id,'rsvpConfig','main'));const data=snap.exists()?(snap.data()||{}):{};fill(doc,config(data.musicConfig||{}));integration(doc,String(data.token||''));}
async function save(doc){const ctx=getWeddingContext();if(!ctx?.id||ctx.legacyMode)throw new Error('No hay una boda activa.');const ref=fsDoc(db,'weddings',ctx.id,'rsvpConfig','main');const snap=await getDoc(ref);const token=String(snap.data()?.token||'').trim();const c=readUi(doc);const batch=writeBatch(db);batch.set(ref,{musicConfig:c,updatedAt:serverTimestamp()},{merge:true});if(token)batch.set(fsDoc(db,'publicRsvp',token),{musicConfig:c,updatedAt:serverTimestamp()},{merge:true});await batch.commit();fill(doc,c);integration(doc,token);}
async function copy(value,button){if(!value)return;await navigator.clipboard.writeText(value);const before=button.textContent;button.textContent='Copiado ✓';setTimeout(()=>button.textContent=before,900);}

function install(doc){
  const card=doc?.getElementById('mgdMusicBuilderCard');if(!card||done.has(doc))return false;done.add(doc);
  ['mgdMusicTitle','mgdMusicIntro','mgdMusicMaxSongs','mgdMusicMessageLabel','mgdMusicEnabled','mgdMusicAskArtist','mgdMusicAskMessage'].forEach(id=>doc.getElementById(id)?.addEventListener('input',()=>preview(doc)));
  const saveButton=doc.getElementById('mgdSaveMusicConfig');saveButton?.addEventListener('click',async()=>{const status=doc.getElementById('mgdMusicBuilderState');saveButton.disabled=true;saveButton.textContent='Guardando…';try{await save(doc);if(status){status.textContent='Encuesta publicada ✓';status.className='rsvp-sync-state is-success';}}catch(error){console.error(error);if(status){status.textContent=error?.message||'No se pudo guardar';status.className='rsvp-sync-state is-error';}}finally{saveButton.disabled=false;saveButton.textContent='Guardar encuesta de música';}});
  const copyUrl=doc.getElementById('mgdCopyMusicUrl');copyUrl?.addEventListener('click',()=>copy(doc.getElementById('mgdMusicPublicUrl')?.value,copyUrl));
  const copyEmbed=doc.getElementById('mgdCopyMusicEmbed');copyEmbed?.addEventListener('click',()=>copy(doc.getElementById('mgdMusicEmbedCode')?.value,copyEmbed));
  doc.getElementById('mgdOpenMusicUrl')?.addEventListener('click',()=>{const url=doc.getElementById('mgdMusicPublicUrl')?.value;if(url)window.open(url,'_blank','noopener');});
  load(doc).catch(console.error);return true;
}
function scan(){const workspace=document.getElementById('unifiedWorkspace');const docs=[document];workspace?.querySelectorAll('iframe').forEach(frame=>{try{if(frame.contentDocument)docs.push(frame.contentDocument);}catch(_){}});docs.forEach(install);}
function bind(){const workspace=document.getElementById('unifiedWorkspace');if(workspace&&workspace.dataset.mgdMusicBuilderObserver!==VERSION){workspace.dataset.mgdMusicBuilderObserver=VERSION;new MutationObserver(scan).observe(workspace,{childList:true});workspace.querySelectorAll('iframe').forEach(frame=>frame.addEventListener('load',scan));}scan();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();

